# 2026 W30 — 週進度

> 做 4 休 2。工作日只寫筆記，兩個休息日才動手。這頁記「這週實際發生了什麼」。

## 本週目標
- [ ] 打通一題 stack overflow（ret2win）本地 → remote
- [ ] 讀懂 `checksec` 六個保護欄位各自擋什麼
- [ ] heap 筆記：tcache 的 fd 指標長怎樣

## 實作紀錄
### 休息日 1
- 環境：WSL2 Ubuntu，`pwndbg` 已裝好，`gdb ./chall` 能斷點。
- 卡點：`p32()` 打過去 offset 對不上 → 用 `cyclic 200` + `cyclic -l 0x6161616c` 找到真正偏移是 **140**。
- 心得：偏移用猜的是浪費時間，`cyclic` 一步到位。

### 休息日 2
- 換 remote：`process('./chall')` → `remote('host', 1337)`，同一份腳本只改這行就通。
- cooldown：今天不開新題，只把上面的過程寫成 writeup。

## 卡兩個學習日的東西（縮小問題）
- （若某點卡超過兩個學習日 → 記在這，下週把它切成更小的子題）

## 下週待辦
- heap first-fit / tcache 實作一次 double free
