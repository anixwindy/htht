# YYYY-MM-DD — 工作回報 #N：【換成一句話標題】

> **給 4 天後的我**：只讀「交接區」那四段就能直接開工，不用回想。

## 交接區 —— 給 4 天後的我

### 現在做到哪

（一句話。**狀態**，不是流水帳。例：方塊能被方向鍵推動，還沒有碰撞。）

### 現在能跑嗎

**能 / 不能。**

能的話，寫出照做就會發生的步驟（寫不出來 = 地板其實沒鎖住）：

1. 開 `<Game>/<Game>.sln`
2. 按 F5
3. 會看到：（具體畫面 / 具體行為）

### 下次第一件事做什麼

（**只寫一件**，具體到不用思考就能開始。
例：讓方塊碰到視窗邊緣停下 —— 在 `Update()` 裡把 `position.X` 夾在 0 和 `Window.ClientBounds.Width - size` 之間。）

### 卡住的地方 / 想不通的

- **跑不動**（錯誤訊息 / 環境問題）：
  （這種是摩擦，**卡超過兩個學習日就縮小問題**，不要硬啃。）
- **想不通**（跑得動，但不知道為什麼會動）：
  （這種是理解缺口，是好題目，下次可以追。）

---

## 紀錄區 —— 給站上清單

### 這個窗做了什麼

（**差分**：這兩天多出來的可見結果。清單越長 = 進步看得見。
條列直接照下面的格式往下加，不用的行刪掉。）

- 項目
- 項目
  - 子項目（縮排 2 空格）

或用有序清單：

1. 項目
2. 項目

### 有用的連結

- [標題](網址)
- （或直接貼網址）

### 地板狀態

- 專案：`<Game>/`
- 這一版有沒有 export / 上線：是（網址：　　）/ 否

<!-- ==========================================================
 條列寫法備忘（這段是註解，網頁上看不到，可以一直留著）

 一、清單一定要用這兩種寫法之一：
     減號 + 一個空格 + 內容        例：  - 修好了 mgcb 的路徑
     數字 + 一個點 + 一個空格      例：  1. 修好了 mgcb 的路徑

 二、四個最常踩的雷（本站 marked 設定 breaks 為 false）：

     雷1  數字後面漏掉那個點
          寫「1 修好了」不算清單，連續好幾行會被黏成一整段。

     雷2  清單上面沒空一行
          會被吸進上一段變成同一句。清單前面一定要有一行空白。

     雷3  分隔線（三個減號）上面沒空一行
          上一行文字會被當成標題語法，整行變成大標題，線也不見了。

     雷4  子項目縮排用了 tab 或一個空格
          要縮 2 個空格才會變成第二層。

 三、想在文章裡秀出一段程式碼，用三個反引號包起來，開頭那行可以標語言：
     ```csharp  ... 你的 code ...  ```
=========================================================== -->
